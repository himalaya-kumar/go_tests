package maath

import (
	"testing"
)

func TestSecondHandAtMidnight(t *testing.T) {

	// tm := time.Date(1337, time.January, 1, 0, 0, 0, 0, time.UTC)
	// want := clockface.Point{X: 150, Y: 150 - 50}
	//
	// // got := clockface.SecondHand(tm)
	// got := ""
	// if got != want {
	// 	t.Errorf("Got %v, wanted %v", got, want)
	// }
}
